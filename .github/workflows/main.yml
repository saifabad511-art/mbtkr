name: Build Android APK
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Unzip and prepare files
        run: |
          echo "جاري فك ضغط ملفات المشروع..."
          unzip -o *.zip
          
          if [ ! -f "build.gradle.kts" ] && [ ! -f "build.gradle" ]; then
            subfolder=$(find . -maxdepth 2 -name "build.gradle*" -exec dirname {} \;)
            if [ -n "$subfolder" ] && [ "$subfolder" != "." ]; then
              echo "نقل الملفات من: $subfolder"
              cp -r $subfolder/* .
            fi
          fi

      - name: Build APK with Auto-Fix Flags
        run: |
          # إضافة بارامترات لتخطي مشاكل التوافقية والتحذيرات الصارمة في Gradle
          if [ -f "gradlew" ]; then
            chmod +x gradlew
            ./gradlew assembleDebug --stacktrace --no-daemon -Pandroid.skipLint=true -Pandroid.experimental.extensions.stringConcat=true
          elif [ -f "gradle/wrapper/gradle-wrapper.jar" ]; then
            java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain assembleDebug --stacktrace --no-daemon -Pandroid.skipLint=true
          else
            gradle assembleDebug --stacktrace --no-daemon -Pandroid.skipLint=true
          fi

      - name: Upload APK Result
        uses: actions/upload-artifact@v4
        with:
          name: Mubtakir-APK
          path: app/build/outputs/apk/debug/*.apk
